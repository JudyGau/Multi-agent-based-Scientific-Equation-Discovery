{
  "_comment": [
    "**自定义提供商**示例：USTC 大模型公共服务平台（OpenAI Chat Completions 兼容）。",
    "provider 段 'ustc' 不在 factory 的内置表里，是靠本档案的 base_url 接进来的——",
    "换任何自建/第三方兼容端点都照这个形状写，不需要改代码。",
    "",
    "官方使用指南：https://llm.ustc.edu.cn/guide/api-usage/ —— 本档案逐项对齐它：",
    "  基础地址  https://api.llm.ustc.edu.cn/v1（指南写作「基础地址」，并注明以平台页面为准）",
    "  鉴权      Authorization: Bearer sk-...（Key 以 sk- 开头）",
    "  对话接口  POST {base_url}/chat/completions（由客户端按 base_url 自动拼接）",
    "  模型名    deepseek-v4-flash（指南示例用的就是它；provider 段 'ustc' 只是本项目的标签，",
    "            不会被发出去——客户端只把斜杠后面的部分写进 model 字段）",
    "",
    "api_key     密钥；留空则回退环境变量 USTC_API_KEY（变量名由 provider 段派生，",
    "            也可用 api_key_env 字段显式指定）。",
    "base_url    端点（自定义提供商必填），必须是完整 URL：http(s)://<主机>/<路径>，",
    "            不接受裸主机域名；本项目统一写成带 /v1 的 OpenAI 兼容形式。",
    "dialect     请求体方言：openai（默认，不认识的一律不发）/ glm / deepseek / ollama。",
    "            这里不声明：指南只保证 model/messages/temperature/max_tokens/stream，",
    "            角色注册表里的 reasoning_effort 会被静默忽略而不是发出 400；",
    "            若确认它支持 reasoning_effort，改成 \"dialect\": \"deepseek\" 即可。",
    "api_key_required  默认 true（自定义提供商按需要密钥处理）；本地免鉴权服务写 false。",
    "max_tokens  输出上限用 max_tokens 而不是 max_completion_tokens：后者是 OpenAI 较新的",
    "            拼法，兼容实现不一定认。",
    "",
    "自检（平台故障时两种都会失败，失败原因以接口返回的原话为准）：",
    "  curl -H \"Authorization: Bearer sk-...\" https://api.llm.ustc.edu.cn/v1/models",
    "  python -m drsr_420.llm.roles --ping     # 走对话接口，更接近真实用法"
  ],
  "base_url": "https://api.llm.ustc.edu.cn/v1",
  "api_key": "",
  "model": "ustc/deepseek-v4-flash",
  "max_tokens": 65536,
  "temperature": 0.4,
  "top_p": 0.9,
  "frequency_penalty": 0.1
}
