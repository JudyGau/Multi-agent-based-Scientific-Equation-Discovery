{
  "_comment": [
    "阿里云百炼（DashScope）OpenAI 兼容模式。provider 段 'dashscope' 不在 factory 的",
    "内置表里，是靠本档案的 base_url 接进来的——**不需要改代码**。端点属于 Q1",
    "（连接谁），本就归档案管。",
    "",
    "官方文档：https://www.alibabacloud.com/help/zh/model-studio/qwen-api-via-openai-chat-completions",
    "",
    "域名    dashscope.aliyuncs.com 是华北2（北京）的域名，现有域名仍可用，但官方建议",
    "        迁到业务空间专属域名 https://<WorkspaceId>.cn-beijing.maas.aliyuncs.com/",
    "        compatible-mode/v1（性能与稳定性更好）。其它区域域名不同：新加坡为",
    "        dashscope-intl.aliyuncs.com，中国香港为 cn-hongkong.dashscope.aliyuncs.com。",
    "        **API Key 必须与区域匹配**，北京的 Key 配到国际站域名上会失败。",
    "",
    "model   斜杠后面才是真正发出去的模型名（前缀 'dashscope' 只是本项目的标签，",
    "        同时是密钥变量名的派生依据）。可用模型名以百炼控制台为准。",
    "api_key 留空则回退环境变量 DASHSCOPE_API_KEY——'dashscope' 派生的变量名恰好与",
    "        阿里官方一致，所以不需要写 api_key_env。",
    "dialect 刻意不声明 = openai（不认识的一律不发）。理由：DashScope 的思考类参数",
    "        （enable_thinking / reasoning_effort / thinking_budget）官方明确标注为",
    "        **非 OpenAI 标准参数**，应当由 extra_body 显式传入。若改成「deepseek」让",
    "        reasoning_effort 直通，取值会按模型而变——例如 qwen3.8-max 只接受",
    "        low/medium/xhigh，**没有 high**，而角色注册表里配的是 high/low，会直接 400。",
    "        另注：qwen3.8-max 不允许同时设置 reasoning_effort 与 thinking_budget。",
    "输出上限 本档案不设，用平台默认（= 模型最大输出长度）。注意 DashScope 已把",
    "        max_tokens 标为**即将废弃**，新接入请用 max_completion_tokens；且后者的",
    "        语义是「回答 + 思维链之和」，开了思考别设太小。",
    "",
    "extra_body 本项目接端点私有能力的唯一出口：它在方言适配**之后**并入请求体顶层，",
    "        因此不会被方言规则删掉，也能覆盖客户端默认发的字段。这里三项：",
    "  tool_choice = none",
    "        **不许模型调用工具**。客户端对每个请求都附带 tools + tool_choice=auto；",
    "        文献摘要只要文本，而 qwen3.7-flash 会把「读文献并总结」答成一次",
    "        search_paper 调用（content 为空），摘要永远拿不到。",
    "        本档案只服务 summary 角色（= read_paper 的摘要），该角色不再有工具调用",
    "        需求，所以这一项不会误伤别处。",
    "  enable_thinking",
    "        混合思考模型（Qwen3 系列等）的思考开关。不写就跟随模型默认（部分模型",
    "        默认开启思考）。思考 token 按输出 token 计费，不需要时设 false。",
    "  stream_options.include_usage",
    "        **流式输出下必须开**：DashScope 只在最后一个数据块返回 token 用量，且",
    "        include_usage 默认为 false。不开的话客户端拿不到 usage，每次调用的 token",
    "        统计会全是 0（不会报错，但记账与进度统计失真）。",
    "",
    "按角色调思考强度：走这份档案的 extra_body（整档案统一），或在",
    "  config/agents.config.json 的 roles.<role>.params 里写整块 extra_body——",
    "  clone_for_task 是按角色整块替换的，可做到「采样不思考、分析开思考」。",
    "",
    "自检（不联网）：python -m drsr_420.llm.roles --check --llm_config dashscope_qwen3.8-max",
    "探活（真实请求）：python -m drsr_420.llm.roles --ping  --llm_config dashscope_qwen3.8-max"
  ],
  "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
  "api_key": "",
  "model": "dashscope/qwen3.7-flash",
  "temperature": 0.7,
  "top_p": 0.95,
  "extra_body": {
    "tool_choice": "none",
    "enable_thinking": true,
    "stream_options": {
      "include_usage": true
    }
  }
}
